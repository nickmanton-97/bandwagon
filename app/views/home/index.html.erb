<!-- app/views/home/index.html.erb -->

<!-- Navigation -->
<nav class="logged-out">
  <div class="logo-container">
    <img src="images/homepage/bandwagon_logo_large.svg" class="logo">
  </div>
  <div class="nav-links">
    <a href="#" class="link">Sign up</a>
    <a href="#" class="nav-log-in">Log in</a>
  </div>
</nav>

<!-- Main Content -->
<main class="home-main">
  <!-- Hero Heading -->
  <div class="hero-section">
    <h1 class="oversized">Find Your People.<br>
      <span class="emphasized">Make Your Sound.</span>
    </h1>

    <div class="cta-buttons" id="home-cta-buttons">
      <button class="nav-sign-up">Sign Up to get started</button>
      <div class="nav-log-in"><p class="caption">Already have an account? <span class="underlined">Log in</span></p></div>
    </div>
  </div>

  <!-- Graphic / Hero Placeholder -->
  <div class="hero-graphic-container">
    <img src="images/homepage/homepage_placeholder.png" class="hero-graphic" alt="Graphic of musicians collaborating">
  </div>
</main>

<!-- Footer -->
<footer>
  &copy; <%= Time.now.year %> Bandwagon. All rights reserved.
</footer>

<!-- Modals -->
<%= render "shared/sign_up_modal" %>
<%= render "shared/log_in_modal" %>

<!-- JS to toggle modals -->
<script>
  document.addEventListener("DOMContentLoaded", () => {
    const signUpButtons = document.querySelectorAll(".nav-sign-up")
    const logInButtons = document.querySelectorAll(".nav-log-in")
    const signUpModal = document.getElementById("sign-up-modal")
    const logInModal = document.getElementById("log-in-modal")
    const closeButtons = document.querySelectorAll(".modal .close")

    // Open modals
    signUpButtons.forEach(btn => btn.addEventListener("click", e => {
      e.preventDefault()
      signUpModal.style.display = "flex"
      logInModal.style.display = "none"
    }))

    logInButtons.forEach(btn => btn.addEventListener("click", e => {
      e.preventDefault()
      logInModal.style.display = "flex"
      signUpModal.style.display = "none"
    }))

    // Close buttons
    closeButtons.forEach(btn => btn.addEventListener("click", () => {
      btn.closest(".modal").style.display = "none"
    }))

    // Close modal on clicking outside content
    document.querySelectorAll(".modal").forEach(modal => {
      modal.addEventListener("click", e => {
        if(e.target === modal) modal.style.display = "none"
      })
    })
  })
</script>
